import { BrowserRouter as Router, Route, Routes } from 'react-router-dom';

export default function CharFeatures () {
    
    return(
        <div id='char_features'>
            features
        </div>
    )
}